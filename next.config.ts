/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.sanity.io",
      "plus.unsplash.com",
      "images.unsplash.com",
      "next-ecommerce-template-4.vercel.app", 
    ],
  },
};

module.exports = nextConfig;
